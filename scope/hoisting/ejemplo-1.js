console.log("Hola, mi nombre es", nombre);
var nombre = "pamela";
console.log("jaja no hay error");

// Hoisting, es la manera en la que se interpreta js,
// elevando las declaraciones con var, los bloques de clases, de funciones y los import.
// Las DECLARACIONES con var, se elevan y se les asigna undefined por defecto
